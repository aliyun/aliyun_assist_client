/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-i386)
GROUP ( /home/changfeng/Videos/glibc-output/lib/libpthread.so.0 /home/changfeng/Videos/glibc-output/lib/libpthread_nonshared.a )
