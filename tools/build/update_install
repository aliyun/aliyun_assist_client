#!/bin/bash

script_dir=$(dirname "$0")
script_dir="$script_dir/.."

chmod a+x $script_dir/aliyun_assist_update
chmod a+x $script_dir/aliyun_installer
chmod a+x $script_dir/aliyun-service

ln -sf $script_dir/aliyun-service /usr/sbin/aliyun-service

