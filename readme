[介绍]
云助手解决了云计算ECS的批量运维问题，如当用户有多台VM时，
使用云助手可以方便的帮助用户批量执行任务，如升级系统补丁，批量执行脚本，批量执行计划任务等场景。

[编译]
Linux平台运行
cmake .
make

Windows平台运行
cmake .
用vs打开assist.sln

[cli使用]
cli的安装和配置参考cli/readme
创建一个命令
>aliyuncli axt CreateCommand --Name 123 --CommandContent ZWNobyAxMjM= --Type RunBatScript
表示创建一个类型为RunBatScript为命令，
其中ZWNobyAxMjM=为echo 123的base编码

创建一个任务
>aliyuncli axt  CreateTask --CommandId c107ddec-649f-47cb-9f86-ceeffc95277b --InstanceIds "['test-vm-instanceid']"
其中：
c107ddec-649f-47cb-9f86-ceeffc95277b为上述创建的命令id
InstanceIds指的vm的实例id

显示任务执行结果
aliyuncli axt  DescribleTaskResult --taskId xxx  --InstanceIds "['test-vm-instanceid']"
taskId为上述创建task的id